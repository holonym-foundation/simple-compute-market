apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "provisioning.fullname" . }}
  labels:
    {{- include "provisioning.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicaCount }}
  # Recreate strategy: the SQLite PVC is ReadWriteOnce; rolling
  # updates would deadlock waiting for the old pod to release it.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "provisioning.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "provisioning.selectorLabels" . | nindent 8 }}
      annotations:
        # Force pod rollout when config or secrets change
        checksum/config:  {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secrets: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
    spec:
      # fsGroup matches the container's appuser (UID 1000); without it
      # the data volume's mount root is root-owned and SQLite can't write.
      securityContext:
        fsGroup: 1000
      containers:
        - name: provisioning
          image: {{ include "provisioning.image" . }}
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.server.port }}
              protocol: TCP
          env:
            - name: ACTIVE_PROFILES
              value: {{ if .Values.mockMode }}"production,provisioning-secrets,mock"{{ else }}"production,provisioning-secrets"{{ end }}
            - name: CONFIG_DIRECTORY
              value: "/app/config"
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: config
              mountPath: /app/config/config-production.yml
              subPath: config-production.yml
              readOnly: true
            - name: provisioning-secrets
              mountPath: /app/config/config-provisioning-secrets.yml
              subPath: config-provisioning-secrets.yml
              readOnly: true
            - name: ssh-key
              mountPath: /home/appuser/.ssh/id_ed25519
              subPath: id_ed25519
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        # SQLite + lease state. Three modes:
        #   existingClaim set → mount a Terraform-managed PVC by name
        #   persistence.enabled → chart creates and owns the PVC
        #   neither           → emptyDir (pod-lifetime, CI/local only)
        - name: data
          {{- if .Values.persistence.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim | quote }}
          {{- else if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "provisioning.pvcName" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: config
          configMap:
            name: {{ include "provisioning.configMapName" . }}
        - name: provisioning-secrets
          secret:
            secretName: {{ include "provisioning.sshDecryptionKeySecretName" . }}
        - name: ssh-key
          secret:
            secretName: {{ include "provisioning.sshKeySecretName" . }}
            defaultMode: 0400
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}